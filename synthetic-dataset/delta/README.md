# Delta Tables for Synthetic Metadata

This directory is intended for storing the synthetic metadata as Delta tables, ready for use in Microsoft Fabric Lakehouse.

- Use Spark or Fabric to convert the JSON metadata to Delta format.
- Partitioning and schema evolution strategies should be documented in the main README.
