# Pipeline Generator

Service for dynamic generation of ADF and Fabric pipelines based on canonical metadata.

- Python/TypeScript
- Consumes canonical metadata JSON
- Generates ADF pipeline JSON or Fabric dataflow definitions at runtime
